"""
The cado-nfs api: its OpenAPI description, its authentication, its
read-only database views, its monitoring and administration endpoints,
and the web ui that consumes them.

The flask application itself lives one level up, in
cadofactor/api_server.py, together with the TLS and WSGI plumbing.
"""
